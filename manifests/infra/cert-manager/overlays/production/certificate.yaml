---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-cloudopsdays-jp
spec:
  dnsNames:
  - '*.dreamkast.cloudopsdays.jp'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  secretName: wildcard-cloudopsdays-jp
